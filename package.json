{
  "name": "skillmesh",
  "version": "1.0.0",
  "private": true,
  "description": "SkillMesh — CCP 能力通约协议参考实现",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "deploy": "npx wrangler pages deploy . --project-name=skillmesh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "axe-core": "^4.13.0",
    "miniflare": "^4.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
